package runner.configuration.enums;

/**
 * Enumerable describing types of system workload generation.
 */
public enum ClientGeneratorTypeEnum {
	RANDOM,
	FROM_SAMPLE,
	FROM_SAMPLE_WITH_BUDGET,
	FROM_EVENTS
}
